# Do not edit this file in Visual studio code.
# Make files are very picky about tabs before a command
# and something about these editors screws up the tabs
# and spacing.  Always use Vim to edit the file.

output: main.o
    g++ -o foo main.o

main.o: main.cpp
    g++ -c -std=c++11 main.cpp

clean:
    \rm *.o output
